#include "stdafx.h"
#include "stdio.h"
#include "conio.h"

void main(void)
{
int i, primer, sup, n; 
 
 printf(" Introduzca ultimo numero: "); 
scanf("%d", &sup); 
 printf(" Los numeros primos son…: "); 
 for(n=1; n<sup; n++) 
 { 
 primer = 1; 
 for(i=2; i<n; i++) 
 if(n%i == 0) 
 { 
 primer = 0; 
 break; 
 } 
 if(primer) 
 printf("%d\n",n); 
 } 
 getch(); 
 }
